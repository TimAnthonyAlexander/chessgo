# Done tasks

Completed task files, moved here from `docs/open_tasks/`. Each should carry a
`## Outcome` section (what shipped, the SPRT/measurement numbers, what was rejected).
See `docs/open_tasks/README.md` for the convention.
