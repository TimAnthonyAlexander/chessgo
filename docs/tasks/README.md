# Tasks

Short, one-file-per-item backlog for the chessgo engine + platform.

- **`open/`** — still-open work (what + why + where, a few lines each).
- **`done/`** — banked milestones.

The old, sprawling gomachine-era task logs live under
`../../gomachine/engine/docs/{open_tasks,done_tasks}/` (archived, not maintained).
